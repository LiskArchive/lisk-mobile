import { StyleSheet } from 'react-native';

const styles = {
  figure: {
    justifyContent: 'center',
    alignItems: 'center',
    paddingBottom: 20,
  },
  avatar: {
    width: 134,
    height: 138,
    borderRadius: 100,
    overflow: 'hidden',
  },
};

export default StyleSheet.create(styles);
