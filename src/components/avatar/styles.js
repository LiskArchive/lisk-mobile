import { StyleSheet } from 'react-native';

const styles = {
  figure: {
    justifyContent: 'center',
    alignItems: 'center',
    paddingBottom: 20,
  },
  avatar: {
    width: 134,
    height: 138,
  },
};

export default StyleSheet.create(styles);
