import { StyleSheet } from 'react-native';

const styles = {
  figure: {
    justifyContent: 'center',
    alignItems: 'center',
    paddingBottom: 20,
  },
  avatar: {
    overflow: 'hidden',
  },
};

export default StyleSheet.create(styles);
