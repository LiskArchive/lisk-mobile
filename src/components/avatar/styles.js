import { StyleSheet } from 'react-native';

const styles = {
  figure: {
    justifyContent: 'center',
    alignItems: 'center',
    overflow: 'hidden',
  },
  avatar: {
    overflow: 'hidden',
  },
};

export default StyleSheet.create(styles);
