
import { StyleSheet } from 'react-native';

const styles = {
  visible: {
    width: 200,
    height: 3,
    backgroundColor: '#f00',
  },
  hidden: {
    width: 200,
    height: 3,
    backgroundColor: '#0f0',
  },
};

export default StyleSheet.create(styles);
