import { StyleSheet } from 'react-native';

const styles = {
  image: {
    justifyContent: 'center',
    alignItems: 'center',
    marginBottom: 60,
  },
};

export default StyleSheet.create(styles);
