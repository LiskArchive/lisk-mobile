import { StyleSheet } from 'react-native';

const styles = {
  listItemEven: {
    backgroundColor: '#eff4f9',
  },
  listItemOdd: {
    backgroundColor: '#fff',
  },
  noBorder: {
    borderWidth: 0,
    marginTop: -1,
  }
};

export default StyleSheet.create(styles);
