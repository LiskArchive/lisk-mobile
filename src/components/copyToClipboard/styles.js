import { StyleSheet } from 'react-native';

const styles = {
  container: {
    minWidth: '20%',
    flexDirection: 'row',
  },
  icon: {
    marginLeft: 5,
  },
};

export default StyleSheet.create(styles);
