import { mockDefaultApplication } from './mockDefaultApplication';
import { mockDefaultApplicationMeta } from './mockDefaultApplicationMeta';

export const mockCurrentApplication = { ...mockDefaultApplication, ...mockDefaultApplicationMeta };
